---
match:
  distribution: '/Graph-Maker-0\.02\.'
depends:
  configure_requires:
    Devel::Hide: 0
test:
  env:
    DEVEL_HIDE_VERBOSE: 0
    PERL5OPT: -MDevel::Hide
    DEVEL_HIDE_PM: Test::Pod
# Workaround for failing pod test, see https://rt.cpan.org/Public/Bug/Display.html?id=108992
