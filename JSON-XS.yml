---
match:
  distribution: '/JSON-XS-2\.33\.'
  perlconfig:
    version: '^5\.(17\.(10|11)|18\.0)'
patches:
  - SREZIC/patches/JSON-XS-2.33-RT84151.patch
# hash (very) randomization problem
---
match:
  distribution: '/\QJSON-XS-4.04.tar.gz\E'
  perlconfig:
    archname: '^(aarch64|arm64)-'
    uselongdouble: 'define'
pl:
  env:
    PERL_CANARY_STABILITY_NOPROMPT: 1
patches:
  - SREZIC/patches/JSON-XS-4.04-RT154165-SISYPHUS.patch
---
match:
  distribution: '/JSON-XS-\d'
pl:
  env:
    PERL_CANARY_STABILITY_NOPROMPT: 1
