---
match:
  distribution: '/Heap-Simple-Perl-\d'
pl:
  expect:
    - '\QRun Heap::Simple benchmarks during \E.make test.\Q ? [n]'
    - "y\n"
    - '\QBenchmarks against other heap modules that happen to be installed on this machine during \E.make test.\Q ? [n]'
    - "y\n"
