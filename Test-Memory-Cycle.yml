---
match:
  distribution: '/Test-Memory-Cycle-1\.04\.'
depends:
# XXX probably only test_requires, but this is not accepted by the schema
  build_requires:
    CGI: 0
# https://rt.cpan.org/Ticket/Display.html?id=99944
