---
match:
  distribution: '/RDF-RDFa-Parser-1\.097\.tar\.gz'
pl:
  commandline: 'rm -rf inc/Scalar/Util.pm; $PERL Makefile.PL'
# Remove bundled Scalar::Util which causes problems
# with newer Type::Tiny. See
# https://rt.cpan.org/Ticket/Display.html?id=134367
